import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.profitlose.app',
  appName: 'TradebookApp',
  webDir: 'dist/tradebook-app/browser'
};

export default config;
